services:
  - type: web
    name: deploywithrender-front
    runtime: static
    rootDir: deploywithrender_front
    buildCommand: npm install; npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
